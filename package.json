{
  "name": "html-workbench-skill",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build/package-skill.mjs",
    "setup-hooks": "git config core.hooksPath .githooks",
    "check:skill": "npm run build && git diff --exit-code -- skill/open-html-editor",
    "serve": "python3 skill/open-html-editor/scripts/workbench.py serve",
    "test": "npm run build && python3 -m unittest discover -s tests -v"
  }
}
